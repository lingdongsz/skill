# 用户接口（套餐信息 / 积分）

用于查询当前 API Token 绑定用户的套餐列表、积分额度、已消耗积分、有效期和套餐状态。

## 对应脚本路径

- 套餐 / 积分余额查询：可通过 `scripts/utils.py` 的通用请求工具直接调用 `/user/package/usage`（本接口 0 积分），也可直接发送 HTTP 请求；
- 所有业务脚本内部也会用到本接口对应的鉴权和套餐信息逻辑。

## 接口选择规则

`/user/package/usage` 只封装 1 个接口：

- `/user/package/usage`：当前用户的套餐列表 + 积分消耗情况（**不消耗积分**）。
  - **什么时候调用**：
    - 用户问"我还剩多少积分""我的套餐是什么""我的额度够不够""我的套餐什么时候到期"。
    - 准备连续调用大量接口前，帮用户确认积分是否够用（可选的贴心动作）。
    - 收到后端返回「积分不足 / 使用次数已满」错误时，调用此接口告诉用户具体情况。
  - **什么时候不调用**：
    - 纯业务分析、纯咨询类问题（不涉及用户套餐/积分）。
    - 每次调用其他 API 前都查一遍（没必要，反而增加请求），建议按需查询。

## `/user/package/usage` 我的套餐信息

> ⚡ **积分消耗**：**0 积分**（查询自身套餐信息不扣费）

返回当前用户名下所有有效/过期/已用完的套餐列表，按购买时间倒序排列。

### 请求参数

无。通过请求头中的 `Authorization: Bearer <Token>` 自动识别所属用户。

### 返回字段（按接口返回顺序）

> **【最新结构说明】** 外层返回公共字段 + `data` 对象（包含用户信息 `nickName` / `phoneNo` 和套餐数组 `packageList`），每条套餐字段见下方 `packageList[*]`。

#### 外层公共字段

- `code`：状态码，`0` 表示成功，非 0 表示失败
- `msg`：状态信息，成功为 `"success"`
- `data`：响应主体（对象），见下一节
- `timestamp`：服务端时间戳（毫秒）

#### `data` 对象字段

- `nickName`：**用户名 / 昵称**（本次新增），如 `"大猫"`
- `phoneNo`：**电话号码**（本次新增），如 `"13926539075"`
- `packageList`：套餐数组，每一条为一份用户套餐（字段见下一节）

#### `data.packageList[*]` 单个套餐字段

- `settingId`：用户配置 ID（内部主键，一般不展示）
- `tcId`：套餐编码（String / Integer），对照表见下方「套餐类型映射」
- `tcCount`：套餐**总积分额度**（无论是否是「无限制」套餐，都直接显示数据库里的 tc\_count 数值，不会返回 null）
- `useCount`：套餐**已消耗积分**（未使用为 0，不会返回 null）
- `buyDate`：购买时间，ISO 格式（如 `2026-07-01T10:00:00`）
- `deadLine`：过期时间，ISO 格式（如 `2026-10-01T23:59:59`）

> **衍生字段提示**：前端/脚本如需展示「剩余积分」「套餐中文名」「状态」「有效期描述」，请根据下面的映射规则和公式自行计算：
>
> - `remainingPoints = tcCount - useCount`
> - `tcName`（套餐中文名称）：依据 `tcId` 查下方映射表
> - `packageTypeDesc`（套餐类型描述）：依据 `tcId` 查下方映射表
> - `status`（套餐状态）：
>   - **有效**：在有效期内 且（无限制 或 剩余积分 > 0）
>   - **已过期**：当前时间超过 deadLine
>   - **已用完**：在有效期内 但 剩余积分 ≤ 0
> - `validPeriod`（有效期描述）：
>   - 正常套餐：`yyyy-MM-dd ~ yyyy-MM-dd`（如 `2026-07-01 ~ 2026-10-01`）
>   - 无限制套餐（`tcId=99`）：`长期有效`
>   - 日期缺失时返回空字符串 `""`

#### 套餐类型映射（tcId → 含义）

| tcId（整数/字符串） | 套餐中文名称 tcName | 套餐类型描述 packageTypeDesc | 说明                                               |
| -----------: | :------------ | :--------------------- | :----------------------------------------------- |
|            1 | 一个月           | 月度套餐                   | 常规套餐，有效期 30 天（自购买日起）                             |
|            2 | 一季度           | 季度套餐                   | 常规套餐，有效期 90 天                                    |
|            3 | 半年            | 半年套餐                   | 常规套餐，有效期 180 天                                   |
|            4 | 一年            | 年度套餐                   | 常规套餐，有效期 365 天                                   |
|           11 | 一次性加餐包        | 加餐包（可叠加）               | 不设独立有效期，跟随主套餐或设置较长截止时间，可多份叠加                     |
|           99 | 无限制           | 不限量套餐                  | 积分额度极高/内部不限量，tcCount 仍按数据库原值显示（通常为超大数字），不会为 null |
|        其他/未知 | 未知套餐          | 未知类型                   | 兜底展示                                             |

### 返回示例

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "packageList": [
      {
        "settingId": 1,
        "tcId": "99",
        "tcCount": 200000000,
        "buyDate": "2026-07-30T11:33:39",
        "deadLine": "2029-07-30T11:33:40",
        "useCount": 132
      }
    ],
    "phoneNo": "13926539075",
    "nickName": "大猫"
  },
  "timestamp": 1786095449230
}
```

> 上面示例中各套餐的**衍生字段**（前端/脚本自行计算）：
>
> - `tcId=99` → 套餐名「无限制 / 不限量套餐」，剩余积分 `= 200000000 − 132 = 199999868`，状态「有效」，有效期「长期有效」
> - `tcId=2` → 套餐名「一季度 / 季度套餐」，剩余积分 `= 5000 − 1280 = 3720`，状态「有效」，有效期 `2026-07-01 ~ 2026-10-01`
> - `tcId=11` → 套餐名「一次性加餐包 / 加餐包（可叠加）」，剩余积分 `= 1000 − 300 = 700`，状态「有效」，有效期 `2026-08-01 ~ 2027-08-01`
> - `tcId=1` → 套餐名「一个月 / 月度套餐」，剩余积分 `= 1000 − 1000 = 0`，当前时间超过 deadLine，状态「已过期」

## 使用提示

- **用户信息展示**：请首先展示 `data.nickName` 和 `data.phoneNo`（如「用户：大猫（手机号：13926539075）」），再展示套餐信息，方便用户确认身份。
- 一个用户可以同时拥有多份套餐（主套餐 + 多份加餐包 + 无限制），套餐都放在 `data.packageList` 数组里，建议前端列表展示。
- 判断「积分够不够」时，取所有「有效」的套餐，将它们的 `tcCount − useCount`（剩余积分）相加，再和本次需要消耗的积分比较即可。
- 如果所有有效套餐剩余积分都接近或用完，建议提示用户续费或购买加餐包。
- `tcId=99`「无限制」套餐虽然 `tcCount` 是超大数字（如 200000000），但前端显示时可以根据套餐名单独处理为「∞」或「不限量」，不要被数字误导。

## 套餐查询输出规范（AI 回答「我的套餐/积分」类问题时，必须严格按下面的顺序和格式展示）

> **核心：先展示用户信息 → 再逐套餐展示「套餐名 + 积分状态 + 有效期」。**
> 禁止把 `tcCount` / `useCount` 说成「次数」，必须读作「**积分**」。

### 展示顺序（严格按此顺序，避免 AI 乱排）

1. **用户身份信息**（data 顶层字段，必须先展示）：
   - 昵称：`nickName`
   - 手机号：`phoneNo`
   - 示例：**「查询用户：大猫（手机号：13926539075）」**
2. **套餐列表**（遍历 `data.packageList`），每条套餐展示：
   - **套餐名称**（用 `tcId` 查映射表得出，**不直接显示 `tcId` 数字**）：

| `tcId` | 套餐中文名称 | 套餐类型描述 |
| ---: | :--- | :--- |
| 1 | **一个月** | 月度套餐 |
| 2 | **一季度** | 季度套餐 |
| 3 | **半年** | 半年套餐 |
| 4 | **一年** | 年度套餐 |
| 11 | **一次性加餐包** | 加餐包（可叠加） |
| 99 | **无限制** | 不限量套餐 |
| 其他 | **未知套餐** | 未知类型 |

   - **积分额度**：总积分（`tcCount`）、已用积分（`useCount`）、剩余积分（`= tcCount - useCount`，公式本地算）
   - **有效期**：购买时间 `buyDate` ~ 截止时间 `deadLine`；`tcId=99` 可写「长期有效」
   - **状态**：有效 / 已过期 / 已用完（本地判断规则：超过 `deadLine` → **已过期**；否则剩余积分 > 0 → **有效**；否则 → **已用完**）
3. **汇总**：所有「有效」套餐的剩余积分之和，作为用户当前可使用的总积分。
4. 最后按规则走「积分消耗统计」：本接口 0 积分。

### 输出示例（AI 回答用户「我还有多少积分 / 我的套餐是什么」时，按这个格式产出）

> **查询用户：大猫（手机号：13926539075）**
>
> 你当前拥有以下套餐：
>
> | 套餐名    | 类型       |         总积分 |  已用积分 |        剩余积分 | 有效期                      | 状态    |
> | :----- | :------- | ----------: | ----: | ----------: | :----------------------- | :---- |
> | 无限制    | 不限量套餐    | 200,000,000 |   132 | 199,999,868 | 长期有效                     | ✅ 有效  |
> | 一季度    | 季度套餐     |       5,000 | 1,280 |       3,720 | 2026-07-01 ~ 2026-10-01 | ✅ 有效  |
> | 一次性加餐包 | 加餐包（可叠加） |       1,000 |   300 |         700 | 2026-08-01 ~ 2027-08-01 | ✅ 有效  |
> | 一个月    | 月度套餐     |       1,000 | 1,000 |           0 | 2026-03-01 ~ 2026-04-01 | ❌ 已过期 |
>
> **当前可用总积分：200,004,288（3 份有效套餐）**。

## 各接口积分定价表（查询套餐时**建议一并展示**，让用户知道每个功能花多少积分）

> **展示说明**：当 AI 回答完「套餐 + 积分余额」的结论后，把下面这张表也附给用户（不要藏着），
> 让他一眼就能对照「自己还剩多少积分」和「每个分析功能要花多少积分」。
>
> **计价规则**：表中是**单次调用**的消耗。同一个接口调用多次需要累计（例如翻页两次搜索 = 5 × 2 = 10 积分）。
> **注意**：`GET /category/tree` 有本地缓存机制（默认缓存 7 天），**命中缓存时免费**，首次或 `--refresh` 强制刷新才扣 10 积分。

| 接口路径                              | 功能描述                                              | 消耗积分 |
| :-------------------------------- | :------------------------------------------------ | ---: |
| `GET /category/tree`              | 站点类目树（**首次请求 10 积分 / 命中本地缓存免费**；默认缓存 7 天，脚本会自动管理） |   10 |
| `GET /cost/weight/list/cbt`       | 自发货运费重量列表                                         |    8 |
| `GET /items/search`               | 商品查询（分页列表）                                        |    5 |
| `POST /catalogs/search`           | 目录链接查询（分页列表）                                      |    4 |
| `POST /keywords/search`           | 热搜词查询（日度 / 月度）                                    |    3 |
| `GET /item/daily`                 | 商品每日历史数据                                          |    2 |
| `GET /item/monthly`               | 商品每月历史数据                                          |    2 |
| `GET /item/keyword/reverse`       | 商品流量词反查                                           |    2 |
| `GET /catalog/daily`              | 目录链接每日历史数据                                        |    2 |
| `POST /seller/search`             | 店铺查询（分页列表）                                        |    2 |
| `GET /trends/price/list`          | 行业价格分布                                            |    2 |
| `GET /trends/new/items`           | 新品机会指数                                            |    2 |
| `GET /trends/brand/top/brands`    | 竞争品牌（Top Brands）                                  |    2 |
| `GET /trends/brand/top/sellers`   | 竞争店铺（Top Sellers）                                 |    2 |
| `GET /trends/sale/list`           | 行业销量分布                                            |    2 |
| `GET /trends/sold/his`            | 行业销量历史                                            |    2 |
| `GET /trends/statistical`         | 行业趋势统计概览                                          |    2 |
| `GET /trends/store/inventoryType` | 仓储类型分布（FBA/FBM）                                   |    2 |
| `GET /trends/brand/top/items`     | 竞争商品（Top Items）                                   |    2 |
| `GET /cost/weight/filter`         | 运费查询                                              |    1 |
| `GET /rate/{siteId}`              | 站点汇率查询                                            |    1 |
| `GET /item/info`                  | 商品详情概要                                            |    1 |
| `GET /item/review`                | 商品评价信息                                            |    1 |
| `GET /catalog/info`               | 目录链接详情                                            |    1 |
| `GET /user/package/usage`         | 我的套餐信息（查询不消耗积分）                                   |    0 |

> **给 AI 的使用要求**：
>
> 1. 当你调用 `/user/package/usage` 并把套餐结果展示给用户后，**紧接着就把上面这张积分定价表附出来**（不要等用户问"积分怎么算"）。
> 2. 附上之后，再接上面 SKILL.md 里定义的「后续分析引导」话术（8 大能力总述 + 下一步推荐选项），引导用户开始用功能。
> 3. 举例完整结尾：
>    - 先显示：查询用户、套餐表、当前可用总积分（按套餐输出规范）
>    - 再显示：**📊 各接口单次积分定价如下**（上面这张表）
>    - 然后：本次积分消耗统计（本接口 0 积分）
>    - 最后：「这个技能还支持：商品分析、行业与趋势分析、竞争分析、店铺分析、关键词研究、目录链接分析、物流与汇率查询、利润测算，你想从哪块开始？需要我帮你查某个站点的类目、搜某个品类的商品，或者直接做一个市场分析吗？」

## 本模块接口积分明细表

> 下表为本模块包含的所有接口及其单次积分消耗，与 SKILL.md 总表完全一致。
> 本接口查询自身套餐信息，**不消耗积分**，可放心调用。

| 接口路径                      | 功能描述            | 消耗积分 |
| :------------------------ | :-------------- | ---: |
| `GET /user/package/usage` | 我的套餐信息（查询不消耗积分） |    0 |

