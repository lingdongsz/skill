---
name: "ljxp"
description: "使用蓝鲸选品 API 和本地脚本分析 Mercado Libre 商品、类目、关键词、价格、竞品、店铺、品牌、目录链接、运费和汇率。"
version: "2.0.0"
author: "蓝鲸选品"
---

# 蓝鲸选品

用于 Mercado Libre 选品调研、竞品分析、类目判断、关键词研究、价格带分析、店铺分析、品牌格局、目录链接机会、运费测算和汇率换算。

优先使用本 skill 自带脚本和 API 参考完成分析。不要假设用户已经配置 Token；调用接口前先确认是否有可用的 Authorization Token 或 `LJXP_TOKEN` 环境变量。

**执行原则：先澄清意图，再调用脚本。** API 调用消耗积分（1\~10 积分/次），方向查偏会浪费积分。用户意图不明确时，必须先提问确认关键参数，拿到答案后再执行查询。详见下方「意图澄清」章节。

## 前置要求

- 所有 API 请求都需要 Authorization Token。
- 不要在回复中暴露 Token。
- 如果没有 Token，提示用户提供 Token 或配置 `LJXP_TOKEN`。
- 支持站点：`MLM`、`MLB`、`MLC`、`MLA`、`MCO`。
- API 基础地址：`https://xpskills.lingdongsz.com/api`。
- 脚本使用 Python 标准库，无需额外依赖。

## 意图澄清（查询前必读）

> **核心原则：在用户意图不明确时，先问清楚再查，不要盲目调用脚本消耗积分。**

API 调用消耗积分（每次 1\~10 积分不等），查询方向错误会导致积分浪费。因此，在执行任何脚本之前，必须先评估用户是否已提供足够的查询参数。如果缺少关键信息，**先向用户提问确认，拿到答案后再执行查询**。

### 何时需要澄清

当用户消息满足以下**任一**条件时，必须先提问，不可直接执行脚本：

- ❌ 未指定站点（MLM/MLB/MLC/MLA/MCO 中哪个？）
- ❌ 未指定类目或关键词（查什么品类？）
- ❌ 分析目标模糊（如"帮我看看市场" — 看什么？趋势？价格？竞品？）
- ❌ 关键参数缺失（如热搜词查询未给日期、趋势查询未给类目 ID）
- ❌ 多个可能方向并存，不确定用户优先级

### 何时可直接执行

当用户消息已包含明确的站点、类目/关键词、分析目标和必要参数时，直接执行查询，不要反复确认拖延。

### 各分析类型的必填参数清单

> **【关于「类目树」参数的极其重要说明】**
> 后端真实接口 `GET /category/tree` **只接受 `siteId` 一个请求参数**（即下面表格里的「站点」必填项）。
> 推荐补充参数里的 `--depth` / `--search` / `--refresh` **全部是脚本本地后处理能力**，
> 不会也不能传给后端接口，AI 在自己拼接 HTTP 请求时绝对不要带上这几个参数，只发 `siteId` 即可。

| 分析类型   | 必填参数（即真正传给后端接口的参数）       | 推荐补充的「脚本本地处理参数」（不会发给后端）     | 对应脚本                            |
| :----- | :------------------------ | :------------------------------ | :------------------------------ |
| 类目树查询/定位ID | 站点（→ 后端 `siteId`，唯一必填参数） | 打印深度 `--depth`、搜索关键词 `--search`、强制刷新 `--refresh` | `category_tree.py`              |
| 类目机会评估 | 站点、类目ID                   | 关注维度（趋势/价格/竞争/品牌）        | `trends.py`                     |
| 价格带分析  | 站点、类目ID                   | 目标价格范围                    | `trends.py`（--type price\_list） |
| 关键词研究  | 站点、查询类型(日/月)、日期           | 类目ID、搜索词                   | `search_keywords.py`            |
| 竞品分析   | 站点、搜索条件(关键词/类目/店铺ID 至少其一) | 价格范围、销量范围、排序方式           | `search_items.py`               |
| 店铺分析   | 站点、筛选条件(类型/等级/店铺ID 至少其一)  | 具体店铺ID                     | `search_sellers.py`             |
| 品牌格局分析 | 站点、类目ID                   | 关注品牌                       | `trends.py`（--type top\_brands） |
| 目录链接机会 | 站点、搜索条件(关键词/类目 至少其一)      | 跟卖状态、价格范围                 | `catalog_search.py`             |
| 利润测算   | 站点、商品信息(价格/重量)            | 成本、目标利润率                  | `get_item_info.py` + 运费查询       |

> **注意：** 类目 ID 格式为站点前缀 + 数字，如 `MLM458037`。如果用户只给了品类名称（如"手机壳""electronics"）但不知道类目 ID，**优先使用 `category_tree.py --search <关键词>`** 本地过滤定位（**首次请求 10 积分/同站点命中本地缓存免费**，默认缓存 7 天，可用 `--refresh` 强制刷新重花积分）；也可以先用关键词搜索商品，从结果中提取类目 ID，再进行后续分析。

### 澄清提问规范

提问时遵循以下原则：

1. **一次性问完**：不要挤牙膏式地问一个问题等回答再问下一个。一次性列出所有需要确认的信息，减少往返轮次。
2. **给默认值建议**：如果某些参数可以给合理默认值，直接建议，让用户确认即可（如"默认查墨西哥站 MLM，可以吗？"）。
3. **用选项引导**：尽量给出选项，降低用户回答成本（如"你想看趋势概览、价格分布还是竞争格局？"）。
4. **说明为什么需要**：简要说明每个参数的用途，让用户理解为什么需要提供。
5. **不要过度提问**：如果用户已经给了足够信息，不要为了"走流程"而额外提问。只问真正缺失且无法合理默认的关键参数。

### 示例

**模糊请求 → 先澄清：**

> 用户："帮我看看手机壳市场怎么样"
>
> 回复："好的，帮你分析手机壳市场。需要确认几个信息：
>
> 1. **站点**：查哪个国家？墨西哥(MLM)、巴西(MLB)、智利(MLC)、阿根廷(MLA)、哥伦比亚(MCO)？
> 2. **类目**：你有具体的类目 ID 吗？如果没有，我先跑 `category_tree.py --site <站点> --search 手机壳` 用本地过滤定位类目 ID（注意：后端 /category/tree 接口只传 siteId，`--search` 是脚本本地后处理，不发给后端）。
> 3. **分析维度**：你想看哪方面？行业趋势概览、价格分布、竞争店铺、还是品牌格局？或者全都要？"

**明确请求 → 直接执行：**

> 用户："查墨西哥站 MLM 类目 MLM458037 的价格分布"
>
> 直接执行：`python trends.py --type price_list --site MLM --category-id MLM458037`

## 分析流程

> 以下每个分析流程的第一步均为「确认关键参数」。请参照上方「意图澄清」章节的必填参数清单，确保用户已提供所需信息后再执行后续步骤。信息不足时，先提问澄清。

### 类目树查询 / 定位类目 ID

当用户只给了品类名称（如"手机壳""耳机""连衣裙""electronics"）或模糊描述，不知道具体的类目 ID 时，先通过这一步定位 ID，否则下游趋势/搜索接口无法使用。

**【极其重要：参数边界】**
- 后端真实接口 `GET /category/tree` **只接受一个参数：siteId（站点）**。AI 拼接 HTTP 请求时绝对不要带 depth / keyword / search / page 之类参数，否则后端报错。
- `--search` / `--depth` / `--output` / `--refresh` / `--cache-ttl` **全都是脚本本地后处理参数**：只在脚本内部对已经拿到的完整数据做「过滤/打印/导出/缓存管理」，不会发 HTTP 请求、不会扣积分。

**本地缓存策略（方案 C）：**脚本默认先查本地缓存，相同站点只在**首次**调用时花 10 积分拉取整棵树并写入 `skills/ljxp-skills/cache/category_tree_<站点>.json`；**后续相同站点的查询（无论加不加 --search/--depth）全部直接读缓存，不调用后端、不消耗积分**。默认 7 天自动过期，也可以随时 `--refresh` 强制重新拉取（本次花 10 积分覆盖旧缓存）。缓存命中时，甚至可以省略 `--token`。

1. **确认关键参数**：站点（必选，唯一会传给后端的值）。本地处理可选：关键词 `--search`（推荐，避免打印整棵树）、深度 `--depth`（默认 3 层）、是否 `--refresh` 强制刷新。
2. 执行 `category_tree.py`：
   - 给了关键词：`python category_tree.py --site MLM --search 手机壳`（命中缓存直接免费出结果；不命中才花 10 积分调一次后端，同站点下次就免费）
   - 没给关键词、想浏览所有类目：`python category_tree.py --site MLM --depth 3`（--depth 只控制本地打印展示，不参与后端请求）
   - 想拿完整 JSON 做本地处理：加 `--output json`
   - 后端加了新类目想更新缓存：`python category_tree.py --token <TOKEN> --site MLM --refresh`（**唯一会真正再次请求后端的场景**，花 10 积分）
3. 从结果中找到用户目标品类的 `id`（形如 `MLM458037`），同时让用户确认名称（英文名 + 中文名）是否对得上，避免 ID 用错。
4. 把 `id` 传给下面任意一个分析流程作为 `--category-id`。

> **积分提示**：同站点首次 10 积分，之后换 100 个关键词搜索、看任意深度都**免费**。尽量不要随便 `--refresh`。

### 类目机会评估

1. **确认关键参数**：站点、类目 ID（或用于定位类目的关键词——没有 ID 就先走上面的「类目树查询」）。
2. 查询行业趋势、价格分布、头部店铺、头部品牌和代表商品。
3. 判断市场容量、竞争强度、品牌集中度、本土/跨境卖家格局。
4. 输出结论：推荐、谨慎、暂不建议。
5. 给出下一步动作：价格、关键词、竞品或上架策略。

### 价格带分析

1. **确认关键参数**：站点、类目 ID。如用户有目标价格范围也一并确认。
2. 查询类目价格分布。
3. 对比各价格区间的商品数量和销量。
4. 找出需求高、供给相对少的价格带。
5. 拉取对应价格区间的热门商品验证实际形态。
6. 输出建议定价、避开价格带和参考商品。

### 关键词研究

1. **确认关键参数**：站点、查询类型（按日 day / 按月 month）、查询日期（如 2026-08-01）。如有特定类目或搜索词也一并确认。
2. 查询日度或月度热搜词。
3. 优先关注搜索量高、访问量高、销量好、商品数较少的词。
4. 用商品搜索验证关键词下的供给和竞争。
5. 输出自然流量词、广告词、长尾词和不建议优先投入的词。

### 竞品分析

1. **确认关键参数**：站点、搜索条件（关键词 / 类目 ID / 店铺 ID 至少提供其一）。推荐补充：价格范围、销量范围、排序方式。
2. 按关键词、类目、销量、评分、评论数、价格、上架时间筛选商品。
3. 对比价格、30天销量、评分、评论数、店铺类型、仓储方式和上架时间。
4. 查看商品详情、评价、每日/月度数据和流量词。
5. 总结爆款共性、用户痛点、差异化机会和风险。

### 店铺分析

1. **确认关键参数**：站点、筛选条件（店铺类型 / 等级 / 优质卖家等级 / 具体店铺 ID 至少提供其一）。
2. 按站点、店铺类型、等级或优质卖家等级筛选店铺。
3. 查看店铺商品数、销量、信用等级、取消率、延迟率、投诉率等指标。
4. 按店铺 ID 查询其商品矩阵。
5. 输出对标店铺、经营模型和可学习点。

### 品牌格局分析

1. **确认关键参数**：站点、类目 ID。如有关注品牌也一并确认。
2. 查询类目头部品牌。
3. 对比品牌数量、活跃品牌数、Top 品牌销量、销售额和均价。
4. 判断是否存在强品牌壁垒。
5. 输出白牌机会、对标品牌和进入风险。

### 目录链接机会

1. **确认关键参数**：站点、搜索条件（关键词 / 类目 ID 至少提供其一）。推荐补充：跟卖状态、价格范围、BSR 范围。
2. 按关键词、类目、销量、BSR、价格、评分、重量或跟卖状态筛选目录链接。
3. 查看目录链接详情和跟卖商品。
4. 判断是否适合跟卖、差异化上架或作为竞品参考。
5. 输出机会链接、风险点和筛选条件。

### 利润测算

1. **确认关键参数**：站点、商品信息（商品 ID 或价格 + 重量）。推荐补充：成本、目标利润率。如用户未提供成本，先询问。
2. 获取商品价格、重量、店铺类型、佣金和货币信息。
3. 查询汇率和运费。
4. 在用户提供成本后，估算毛利、毛利率和价格安全区间。
5. 明确说明测算假设。

## 常用脚本

- 站点类目树查询 / 类目 ID 定位（**同站点首次 10 积分，本地缓存后续免费**）：`scripts/category_tree.py`
- 商品搜索：`scripts/search_items.py`
- 商品详情、每日数据、每月数据、评价、流量词反查：`scripts/get_item_info.py`
- 店铺搜索：`scripts/search_sellers.py`
- 热搜词查询：`scripts/search_keywords.py`
- 目录链接查询：`scripts/catalog_search.py`
- 行业趋势查询：`scripts/trends.py`
- 通用请求、鉴权和格式化工具：`scripts/utils.py`

## 参考资料

- 完整 API 和响应字段：`references/api_reference.md`
- 分模块 API 文档：
  - **类目树**：`references/api/category.md`
  - 商品：`references/api/items.md`
  - 热搜词：`references/api/keywords.md`
  - 店铺：`references/api/sellers.md`
  - 行业趋势：`references/api/trends.md`
  - 目录链接：`references/api/catalogs.md`
  - 汇率与运费：`references/api/rate-shipping.md`
  - **用户套餐 / 积分**：`references/api/users.md`
- 搜索结果 HTML 模板：`references/search_results_template.html`

## 接口积分消耗表

每次调用 API 都会按以下标准消耗积分（默认 1 积分），回答结束时必须汇总并告知用户。

| 接口路径                              | 功能描述              | 消耗积分 |
| :-------------------------------- | :---------------- | ---: |
| `GET /category/tree`              | 站点类目树（**首次请求 10 积分 / 命中本地缓存免费**；默认缓存 7 天，脚本会自动管理） |   10 |
| `GET /cost/weight/list/cbt`       | 自发货运费重量列表         |    8 |
| `GET /items/search`               | 商品查询（分页列表）        |    5 |
| `POST /catalogs/search`           | 目录链接查询（分页列表）      |    4 |
| `POST /keywords/search`           | 热搜词查询（日度/月度）      |    3 |
| `GET /item/daily`                 | 商品每日历史数据          |    2 |
| `GET /item/monthly`               | 商品每月历史数据          |    2 |
| `GET /item/keyword/reverse`       | 商品流量词反查           |    2 |
| `GET /catalog/daily`              | 目录链接每日历史数据        |    2 |
| `POST /seller/search`             | 店铺查询（分页列表）        |    2 |
| `GET /trends/price/list`          | 行业价格分布            |    2 |
| `GET /trends/new/items`           | 新品机会指数            |    2 |
| `GET /trends/brand/top/brands`    | 竞争品牌（Top Brands）  |    2 |
| `GET /trends/brand/top/sellers`   | 竞争店铺（Top Sellers） |    2 |
| `GET /trends/sale/list`           | 行业销量分布            |    2 |
| `GET /trends/sold/his`            | 行业销量历史            |    2 |
| `GET /trends/statistical`         | 行业趋势统计概览          |    2 |
| `GET /trends/store/inventoryType` | 仓储类型分布（FBA/FBM）   |    2 |
| `GET /trends/brand/top/items`     | 竞争商品（Top Items）   |    2 |
| `GET /cost/weight/filter`         | 运费查询              |    1 |
| `GET /rate/{siteId}`              | 站点汇率查询            |    1 |
| `GET /item/info`                  | 商品详情概要            |    1 |
| `GET /item/review`                | 商品评价信息            |    1 |
| `GET /catalog/info`               | 目录链接详情            |    1 |
| `GET /user/package/usage`         | 我的套餐信息（查询不消耗积分）   |    0 |

> 注意：同一个接口调用多次需累计计算（例如翻页时两次搜索 = 5 × 2 = 10 积分）。

## 输出要求

分析结果优先给业务结论，不要只堆原始数据。

通常包含：

- 关键数据
- 机会等级
- 主要风险
- 推荐动作
- 使用的筛选条件或脚本
- 需要用户补充的信息

### 套餐查询输出规范（调用 /user/package/usage 时适用）

> **核心：先展示用户信息 → 再逐套餐展示「套餐名 + 积分状态 + 有效期」。** 禁止把 `tcCount` / `useCount` 说成「次数」，必须读作「**积分**」。

**展示顺序**（严格按此顺序，避免 AI 乱排）：

1. **用户身份信息**（data 顶层新增字段，必须先展示）：
   - 昵称：`nickName`
   - 手机号：`phoneNo`
   - 示例：**「查询用户：大猫（手机号：13926539075）」**
2. **套餐列表**（遍历 `data.packageList`），每条套餐展示：
   - **套餐名称**（用 `tcId` 查映射表得出，不直接显示 `tcId` 数字）：
     - `tcId=1` → **一个月**（月度套餐）
     - `tcId=2` → **一季度**（季度套餐）
     - `tcId=3` → **半年**（半年套餐）
     - `tcId=4` → **一年**（年度套餐）
     - `tcId=11` → **一次性加餐包**（加餐包，可叠加）
     - `tcId=99` → **无限制**（不限量套餐）
     - 其他 → **未知套餐**
   - **积分额度**：总积分（`tcCount`）、已用积分（`useCount`）、剩余积分（= tcCount − useCount，公式本地算）
   - **有效期**：购买时间 `buyDate` ~ 截止时间 `deadLine`；`tcId=99` 可写「长期有效」
   - **状态**：有效 / 已过期 / 已用完（本地判断：超过 deadLine = 已过期；否则剩余积分 > 0 = 有效；否则 = 已用完）
3. **汇总**：所有「有效」套餐的剩余积分之和，作为用户当前可使用的总积分。
4. 最后按规则走「积分消耗统计」：本接口 0 积分。

**输出示例（当用户查套餐时）**：
> **查询用户：大猫（手机号：13926539075）**
>
> 你当前拥有以下套餐：
>
> | 套餐名 | 类型 | 总积分 | 已用积分 | 剩余积分 | 有效期 | 状态 |
> |:---|:---|---:|---:|---:|:---|:---|
> | 无限制 | 不限量套餐 | 200,000,000 | 132 | 199,999,868 | 长期有效 | ✅ 有效 |
> | 一季度 | 季度套餐 | 5,000 | 1,280 | 3,720 | 2026-07-01 ~ 2026-10-01 | ✅ 有效 |
> | 一次性加餐包 | 加餐包（可叠加） | 1,000 | 300 | 700 | 2026-08-01 ~ 2027-08-01 | ✅ 有效 |
> | 一个月 | 月度套餐 | 1,000 | 1,000 | 0 | 2026-03-01 ~ 2026-04-01 | ❌ 已过期 |
>
> **当前可用总积分：200,004,288（3 份有效套餐）**。

### 积分消耗统计

**每次回答结束后，必须附加以下格式的积分消耗统计：**

```
---
### 📊 本次积分消耗

| 功能 | 调用次数 | 单次积分 | 小计 |
|:---|---:|---:|---:|
| 商品查询 | 1 | 5 | 5 |
| 商品每日数据 | 2 | 2 | 4 |
| 店铺查询 | 1 | 2 | 2 |

**合计消耗：11 积分**
```

规则说明：

1. 按「功能名」列出本次回答中实际调用过的所有接口功能（没用到的不要列）。
2. 同一个接口调用多次（如翻页、多站点、多商品）需累计次数和小计。
3. 功能名使用上表中的「功能描述」，可简写但要明确对应哪个接口；括号里的路径可选，推荐写上。
4. **类目树的特殊规则（方案 C 本地缓存）**：
   - 本次 `category_tree.py` 如果打印了「命中本地缓存」，本次**不记积分**（调用次数填 1、单次积分填 0，小计 0），并在功能名备注「(本地缓存命中，免费)」。
   - 如果打印了「本次消耗 10 积分」或用了 `--refresh`，本次**按 10 积分计算**。
5. 最后必须有「合计消耗：X 积分」的汇总行。
6. 如果本次没有调用任何接口（纯咨询、纯方案），说明「本次未调用 API，不消耗积分」。

### HTML 结果生成策略

> **核心原则：用户没有明确要求时，不主动生成 HTML。**

1. **默认行为：不生成 HTML。** 回答结束后以 Markdown 表格 + 文字结论的形式直接交付结果。不要为了"好看"或"方便对比"私自调用 `references/search_results_template.html` 生成 HTML。
2. **询问时机**：在完成业务分析结论 + 积分消耗统计后，如果本次调用涉及列表类结果（商品搜索、店铺搜索、目录链接、热搜词、价格分布等有 5 条以上数据），可以在回答末尾**主动询问**用户：
   > 「需要为你生成 HTML 格式的对比结果（可直接在浏览器打开）吗？」
   涉及数据较少（≤3 条记录）或纯文字结论（没有表格）的场景，没必要询问。
3. **只有以下情况才生成 HTML**：
   - 用户消息里明确说「生成 HTML」「导出 HTML」「给我一个能直接打开的页面」「做个可视化对比」。
   - 回答末尾主动询问后，用户回复「要 / 生成 / 来吧」之类的确认。
4. **生成 HTML 时的规则**：
   - 使用 `references/search_results_template.html` 作为模板。
   - 模板的使用说明和数据注入方式见模板文件顶部注释。
   - 生成 HTML 后，把文件放在用户工作目录（不要放在 skill 内部目录），并给出可点击的绝对路径链接。
   - 生成 HTML 本身**不调用任何 API**，因此不额外消耗积分（积分只在前面的 API 调用时已经计算过）。

